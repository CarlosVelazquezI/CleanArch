import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
 
    input: {
        height: 40,
        margin: 12,
        borderWidth: 1,
        padding: 10,
        borderRadius:50,
        backgroundColor:'#fff'
      },
    text:{
        paddingHorizontal: 10,
    }
  });


export { styles };